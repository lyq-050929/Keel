PORT=8090
DATA_DIR=./data
APP_ENV=dev
REQUEST_TIMEOUT=30s
SHUTDOWN_TIMEOUT=10s

# Optional. If empty, the service uses local file persistence for session memory.
REDIS_URL=

# Optional. When set, API requests must send X-API-Key or Authorization: Bearer.
API_KEY=
